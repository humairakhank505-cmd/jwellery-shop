<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Lumière Jewels — Timeless Elegance, Crafted for You</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  /* ---------- Reset & Base ---------- */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  :root {
    --bg: #faf7f2;
    --bg-dark: #0e0e0e;
    --text: #1a1a1a;
    --muted: #6b6b6b;
    --gold: #c9a961;
    --gold-dark: #a8874a;
    --line: #e6dfd2;
    --white: #ffffff;
    --serif: 'Cormorant Garamond', Georgia, serif;
    --sans: 'Inter', system-ui, sans-serif;
  }
  html { scroll-behavior: smooth; }
  body {
    font-family: var(--sans);
    color: var(--text);
    background: var(--bg);
    line-height: 1.6;
    overflow-x: hidden;
  }
  img { max-width: 100%; display: block; }
  a { color: inherit; text-decoration: none; }
  button { font: inherit; cursor: pointer; border: none; background: none; color: inherit; }

  /* ---------- Utility ---------- */
  .container { max-width: 1280px; margin: 0 auto; padding: 0 24px; }
  .eyebrow {
    font-size: 12px; letter-spacing: 4px; text-transform: uppercase;
    color: var(--gold); font-weight: 500;
  }
  .section-title {
    font-family: var(--serif);
    font-size: clamp(32px, 4vw, 52px);
    font-weight: 500;
    letter-spacing: 0.5px;
    line-height: 1.15;
    margin: 12px 0 16px;
  }
  .section-sub {
    color: var(--muted); max-width: 560px; margin: 0 auto 48px;
    text-align: center;
  }
  .center { text-align: center; }

  /* ---------- Announcement Bar ---------- */
  .announce {
    background: var(--bg-dark); color: #e9e3d4;
    text-align: center; padding: 10px 16px;
    font-size: 12px; letter-spacing: 2px; text-transform: uppercase;
  }
  .announce span { color: var(--gold); }

  /* ---------- Header / Nav ---------- */
  header {
    position: sticky; top: 0; z-index: 100;
    background: rgba(250, 247, 242, 0.92);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--line);
    transition: box-shadow 0.3s ease;
  }
  header.scrolled { box-shadow: 0 4px 24px rgba(0,0,0,0.05); }
  .nav {
    display: flex; align-items: center; justify-content: space-between;
    padding: 18px 0;
  }
  .logo {
    font-family: var(--serif);
    font-size: 28px; font-weight: 600;
    letter-spacing: 2px;
  }
  .logo span { color: var(--gold); }
  .nav-links {
    display: flex; gap: 36px; list-style: none;
  }
  .nav-links a {
    font-size: 14px; letter-spacing: 1px; text-transform: uppercase;
    position: relative; padding: 6px 0;
    transition: color 0.25s;
  }
  .nav-links a::after {
    content: ''; position: absolute; left: 0; bottom: 0;
    width: 0; height: 1px; background: var(--gold);
    transition: width 0.3s ease;
  }
  .nav-links a:hover { color: var(--gold); }
  .nav-links a:hover::after { width: 100%; }
  .nav-icons { display: flex; gap: 20px; align-items: center; }
  .icon-btn {
    width: 40px; height: 40px; display: grid; place-items: center;
    border-radius: 50%; transition: background 0.25s;
    position: relative;
  }
  .icon-btn:hover { background: rgba(201, 169, 97, 0.1); }
  .icon-btn svg { width: 20px; height: 20px; stroke: var(--text); }
  .cart-count {
    position: absolute; top: 2px; right: 2px;
    background: var(--gold); color: white;
    font-size: 10px; font-weight: 600;
    width: 18px; height: 18px; border-radius: 50%;
    display: grid; place-items: center;
  }
  .menu-toggle { display: none; }

  /* ---------- Hero ---------- */
  .hero {
    position: relative; min-height: 88vh;
    display: flex; align-items: center;
    background:
      linear-gradient(135deg, rgba(14,14,14,0.55), rgba(14,14,14,0.25)),
      url('https://images.unsplash.com/photo-1611652022419-a9419f74343d?auto=format&fit=crop&w=1920&q=80') center/cover no-repeat;
    color: white;
    overflow: hidden;
  }
  .hero-content { max-width: 640px; padding: 80px 0; }
  .hero h1 {
    font-family: var(--serif);
    font-size: clamp(44px, 6vw, 84px);
    font-weight: 400;
    line-height: 1.05;
    margin: 20px 0 24px;
    letter-spacing: 1px;
  }
  .hero h1 em { color: var(--gold); font-style: italic; }
  .hero p {
    font-size: 17px; max-width: 480px;
    margin-bottom: 36px; opacity: 0.92;
  }
  .btn {
    display: inline-block; padding: 16px 36px;
    font-size: 13px; letter-spacing: 2px; text-transform: uppercase;
    font-weight: 500; border: 1px solid var(--gold);
    background: var(--gold); color: white;
    transition: all 0.3s ease;
    cursor: pointer;
  }
  .btn:hover { background: transparent; color: var(--gold); }
  .btn-outline {
    background: transparent; color: white; border-color: white;
  }
  .btn-outline:hover { background: white; color: var(--bg-dark); }
  .btn-dark { background: var(--bg-dark); border-color: var(--bg-dark); }
  .btn-dark:hover { background: transparent; color: var(--bg-dark); }
  .hero-btns { display: flex; gap: 16px; flex-wrap: wrap; }

  /* ---------- Categories ---------- */
  section { padding: 100px 0; }
  .cat-grid {
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }
  .cat-card {
    position: relative; aspect-ratio: 3/4;
    overflow: hidden; border-radius: 2px;
    cursor: pointer;
  }
  .cat-card img {
    width: 100%; height: 100%; object-fit: cover;
    transition: transform 0.8s ease;
  }
  .cat-card:hover img { transform: scale(1.08); }
  .cat-card::after {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.65) 0%, transparent 50%);
  }
  .cat-label {
    position: absolute; bottom: 24px; left: 24px; right: 24px;
    color: white; z-index: 2;
  }
  .cat-label h3 {
    font-family: var(--serif); font-size: 28px;
    font-weight: 500; margin-bottom: 4px;
  }
  .cat-label span {
    font-size: 12px; letter-spacing: 2px;
    text-transform: uppercase; color: var(--gold);
  }

  /* ---------- Products ---------- */
  .products { background: var(--white); }
  .product-grid {
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 32px 24px;
  }
  .product {
    cursor: pointer;
    transition: transform 0.3s ease;
  }
  .product-img {
    position: relative; aspect-ratio: 1/1;
    overflow: hidden; background: #f0ebe0;
    margin-bottom: 16px;
  }
  .product-img img {
    width: 100%; height: 100%; object-fit: cover;
    transition: transform 0.6s ease;
  }
  .product:hover .product-img img { transform: scale(1.06); }
  .badge {
    position: absolute; top: 12px; left: 12px;
    background: var(--bg-dark); color: white;
    font-size: 10px; letter-spacing: 2px; text-transform: uppercase;
    padding: 6px 10px;
  }
  .badge.gold { background: var(--gold); }
  .quick-add {
    position: absolute; bottom: 0; left: 0; right: 0;
    background: var(--bg-dark); color: white;
    padding: 14px; text-align: center;
    font-size: 12px; letter-spacing: 2px; text-transform: uppercase;
    transform: translateY(100%); transition: transform 0.35s ease;
  }
  .product:hover .quick-add { transform: translateY(0); }
  .product h4 {
    font-family: var(--serif); font-size: 20px;
    font-weight: 500; margin-bottom: 4px;
  }
  .product .price { color: var(--gold); font-weight: 500; font-size: 15px; }
  .product .price s { color: var(--muted); font-weight: 400; margin-left: 8px; font-size: 13px; }

  /* ---------- About / Craftsmanship ---------- */
  .about {
    background: var(--bg-dark); color: var(--bg);
    position: relative; overflow: hidden;
  }
  .about-grid {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 80px; align-items: center;
  }
  .about-img {
    aspect-ratio: 4/5; overflow: hidden;
    border-radius: 2px;
  }
  .about-img img { width: 100%; height: 100%; object-fit: cover; }
  .about h2 { color: white; }
  .about p { color: #c9c2b3; margin-bottom: 20px; font-size: 16px; }
  .stats {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 24px; margin-top: 40px;
    padding-top: 40px; border-top: 1px solid rgba(255,255,255,0.1);
  }
  .stat-num {
    font-family: var(--serif); font-size: 42px;
    color: var(--gold); font-weight: 500;
  }
  .stat-label {
    font-size: 12px; letter-spacing: 2px;
    text-transform: uppercase; color: #9a9385;
    margin-top: 4px;
  }

  /* ---------- Testimonials ---------- */
  .testimonials { background: var(--bg); }
  .testi-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  .testi {
    background: white; padding: 40px 32px;
    border: 1px solid var(--line);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .testi:hover {
    transform: translateY(-6px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.06);
  }
  .stars { color: var(--gold); margin-bottom: 16px; letter-spacing: 2px; }
  .testi p {
    font-family: var(--serif); font-size: 19px;
    font-style: italic; line-height: 1.5;
    margin-bottom: 24px; color: #333;
  }
  .testi-author {
    display: flex; align-items: center; gap: 14px;
    padding-top: 20px; border-top: 1px solid var(--line);
  }
  .avatar {
    width: 48px; height: 48px; border-radius: 50%;
    background: var(--gold); color: white;
    display: grid; place-items: center;
    font-family: var(--serif); font-size: 20px; font-weight: 600;
  }
  .testi-author strong { display: block; font-size: 14px; }
  .testi-author span { font-size: 12px; color: var(--muted); }

  /* ---------- Newsletter ---------- */
  .newsletter {
    background: linear-gradient(rgba(14,14,14,0.7), rgba(14,14,14,0.7)),
      url('https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?auto=format&fit=crop&w=1920&q=80') center/cover;
    color: white; text-align: center;
    padding: 120px 0;
  }
  .newsletter h2 { color: white; }
  .newsletter p { color: #d6cfbf; margin-bottom: 32px; }
  .newsletter-form {
    display: flex; max-width: 520px; margin: 0 auto;
    border: 1px solid rgba(255,255,255,0.3);
  }
  .newsletter-form input {
    flex: 1; padding: 16px 20px;
    background: transparent; border: none;
    color: white; font-size: 14px; outline: none;
  }
  .newsletter-form input::placeholder { color: rgba(255,255,255,0.6); }
  .newsletter-form button {
    padding: 16px 32px; background: var(--gold);
    color: white; font-size: 12px; letter-spacing: 2px;
    text-transform: uppercase; transition: background 0.3s;
  }
  .newsletter-form button:hover { background: var(--gold-dark); }

  /* ---------- Footer ---------- */
  footer {
    background: var(--bg-dark); color: #b8b1a2;
    padding: 80px 0 30px;
  }
  .footer-grid {
    display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 48px; margin-bottom: 60px;
  }
  .footer-brand .logo { color: white; margin-bottom: 16px; display: inline-block; }
  .footer-brand p { font-size: 14px; line-height: 1.7; max-width: 320px; }
  .footer h5 {
    color: white; font-size: 13px; letter-spacing: 2px;
    text-transform: uppercase; margin-bottom: 20px;
  }
  .footer ul { list-style: none; }
  .footer li { margin-bottom: 10px; }
  .footer a { font-size: 14px; transition: color 0.25s; }
  .footer a:hover { color: var(--gold); }
  .socials { display: flex; gap: 12px; margin-top: 20px; }
  .socials a {
    width: 38px; height: 38px; border: 1px solid rgba(255,255,255,0.15);
    border-radius: 50%; display: grid; place-items: center;
    transition: all 0.3s;
  }
  .socials a:hover { border-color: var(--gold); background: var(--gold); color: white; }
  .socials svg { width: 16px; height: 16px; }
  .copyright {
    text-align: center; padding-top: 30px;
    border-top: 1px solid rgba(255,255,255,0.08);
    font-size: 13px; color: #7a7466;
  }

  /* ---------- Cart Drawer ---------- */
  .overlay {
    position: fixed; inset: 0; background: rgba(0,0,0,0.5);
    opacity: 0; pointer-events: none;
    transition: opacity 0.3s; z-index: 200;
  }
  .overlay.open { opacity: 1; pointer-events: auto; }
  .cart-drawer {
    position: fixed; top: 0; right: 0; bottom: 0;
    width: 420px; max-width: 100vw;
    background: white; z-index: 201;
    transform: translateX(100%);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex; flex-direction: column;
  }
  .cart-drawer.open { transform: translateX(0); }
  .cart-header {
    padding: 24px; border-bottom: 1px solid var(--line);
    display: flex; justify-content: space-between; align-items: center;
  }
  .cart-header h3 { font-family: var(--serif); font-size: 24px; font-weight: 500; }
  .close-btn {
    width: 36px; height: 36px; border-radius: 50%;
    display: grid; place-items: center;
    transition: background 0.25s;
  }
  .close-btn:hover { background: var(--line); }
  .cart-items { flex: 1; overflow-y: auto; padding: 20px 24px; }
  .cart-item {
    display: grid; grid-template-columns: 80px 1fr auto;
    gap: 16px; padding: 16px 0;
    border-bottom: 1px solid var(--line);
  }
  .cart-item img { width: 80px; height: 80px; object-fit: cover; }
  .cart-item h5 { font-size: 14px; font-weight: 500; margin-bottom: 4px; }
  .cart-item .price { color: var(--gold); font-size: 14px; font-weight: 500; }
  .qty {
    display: inline-flex; align-items: center;
    border: 1px solid var(--line); margin-top: 8px;
  }
  .qty button {
    width: 28px; height: 28px; font-size: 14px;
    transition: background 0.2s;
  }
  .qty button:hover { background: var(--line); }
  .qty span { padding: 0 10px; font-size: 13px; }
  .remove {
    font-size: 11px; color: var(--muted);
    letter-spacing: 1px; text-transform: uppercase;
    margin-top: 8px; display: block;
    transition: color 0.2s;
  }
  .remove:hover { color: #c0392b; }
  .cart-empty { text-align: center; padding: 60px 20px; color: var(--muted); }
  .cart-footer {
    padding: 24px; border-top: 1px solid var(--line);
    background: var(--bg);
  }
  .cart-total {
    display: flex; justify-content: space-between;
    margin-bottom: 16px; font-size: 16px;
  }
  .cart-total strong { font-family: var(--serif); font-size: 22px; color: var(--gold); }
  .checkout-btn { width: 100%; }

  /* ---------- Mobile Menu ---------- */
  .mobile-menu {
    position: fixed; top: 0; left: 0; bottom: 0;
    width: 320px; max-width: 85vw;
    background: white; z-index: 201;
    transform: translateX(-100%);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    padding: 30px;
  }
  .mobile-menu.open { transform: translateX(0); }
  .mobile-menu ul { list-style: none; margin-top: 40px; }
  .mobile-menu li {
    padding: 16px 0; border-bottom: 1px solid var(--line);
  }
  .mobile-menu a {
    font-family: var(--serif); font-size: 22px;
  }

  /* ---------- Animations ---------- */
  .reveal {
    opacity: 0; transform: translateY(30px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  /* ---------- Responsive ---------- */
  @media (max-width: 960px) {
    .cat-grid { grid-template-columns: repeat(2, 1fr); }
    .product-grid { grid-template-columns: repeat(2, 1fr); }
    .about-grid { grid-template-columns: 1fr; gap: 40px; }
    .testi-grid { grid-template-columns: 1fr; }
    .footer-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 720px) {
    .nav-links { display: none; }
    .menu-toggle { display: grid; }
    section { padding: 70px 0; }
    .hero { min-height: 75vh; }
    .hero-content { padding: 60px 0; }
    .newsletter-form { flex-direction: column; border: none; }
    .newsletter-form input { border: 1px solid rgba(255,255,255,0.3); margin-bottom: 12px; }
    .footer-grid { grid-template-columns: 1fr; gap: 32px; }
  }
</style>
</head>
<body>

<!-- Announcement -->
<div class="announce">Complimentary shipping on orders over <span>$250</span> · Lifetime warranty</div>

<!-- Header -->
<header id="header">
  <div class="container nav">
    <button class="icon-btn menu-toggle" id="menuToggle" aria-label="Menu">
      <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
    </button>
    <a href="#" class="logo">LUMI<span>È</span>RE</a>
    <ul class="nav-links">
      <li><a href="#collections">Collections</a></li>
      <li><a href="#shop">Shop</a></li>
      <li><a href="#about">Craftsmanship</a></li>
      <li><a href="#reviews">Reviews</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <div class="nav-icons">
      <button class="icon-btn" aria-label="Search">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5"><circle cx="11" cy="11" r="7"/><path d="m21 21-4.3-4.3"/></svg>
      </button>
      <button class="icon-btn" aria-label="Account">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 4-7 8-7s8 3 8 7"/></svg>
      </button>
      <button class="icon-btn" id="cartBtn" aria-label="Cart">
        <svg viewBox="0 0 24 24" fill="none" stroke-width="1.5"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4Z"/><path d="M3 6h18M16 10a4 4 0 0 1-8 0"/></svg>
        <span class="cart-count" id="cartCount">0</span>
      </button>
    </div>
  </div>
</header>

<!-- Hero -->
<section class="hero">
  <div class="container hero-content">
    <span class="eyebrow">New Collection · 2026</span>
    <h1>Timeless <em>Elegance,</em><br>Crafted for You.</h1>
    <p>Discover handcrafted fine jewelry made with ethically sourced diamonds and precious metals. Each piece tells a story of artistry and heritage.</p>
    <div class="hero-btns">
      <a href="#shop" class="btn">Shop Collection</a>
      <a href="#about" class="btn btn-outline">Our Story</a>
    </div>
  </div>
</section>

<!-- Categories -->
<section id="collections">
  <div class="container">
    <div class="center reveal">
      <span class="eyebrow">Our Collections</span>
      <h2 class="section-title">Curated for Every Occasion</h2>
      <p class="section-sub">From everyday essentials to statement pieces, explore jewelry designed to be treasured for generations.</p>
    </div>
    <div class="cat-grid reveal">
      <div class="cat-card">
        <img src="https://images.unsplash.com/photo-1605100804763-247f67b3557e?auto=format&fit=crop&w=600&q=80" alt="Rings">
        <div class="cat-label"><span>24 Pieces</span><h3>Rings</h3></div>
      </div>
      <div class="cat-card">
        <img src="https://images.unsplash.com/photo-1599643477877-530eb83abc8e?auto=format&fit=crop&w=600&q=80" alt="Necklaces">
        <div class="cat-label"><span>18 Pieces</span><h3>Necklaces</h3></div>
      </div>
      <div class="cat-card">
        <img src="https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?auto=format&fit=crop&w=600&q=80" alt="Earrings">
        <div class="cat-label"><span>32 Pieces</span><h3>Earrings</h3></div>
      </div>
      <div class="cat-card">
        <img src="https://images.unsplash.com/photo-1611591437281-460bfbe1220a?auto=format&fit=crop&w=600&q=80" alt="Bracelets">
        <div class="cat-label"><span>15 Pieces</span><h3>Bracelets</h3></div>
      </div>
    </div>
  </div>
</section>

<!-- Products -->
<section class="products" id="shop">
  <div class="container">
    <div class="center reveal">
      <span class="eyebrow">Bestsellers</span>
      <h2 class="section-title">Most Loved Pieces</h2>
      <p class="section-sub">Handpicked favorites chosen by our community of jewelry lovers.</p>
    </div>
    <div class="product-grid reveal" id="productGrid"></div>
  </div>
</section>

<!-- About -->
<section class="about" id="about">
  <div class="container about-grid">
    <div class="about-img reveal">
      <img src="https://images.unsplash.com/photo-1617038260897-41a1f14a8ca0?auto=format&fit=crop&w=800&q=80" alt="Craftsman">
    </div>
    <div class="reveal">
      <span class="eyebrow">Our Heritage</span>
      <h2 class="section-title">Crafted with Passion Since 1987</h2>
      <p>For over three decades, Lumière has been synonymous with exceptional craftsmanship and timeless design. Our master artisans blend traditional techniques with contemporary vision to create pieces that transcend trends.</p>
      <p>Every gemstone is ethically sourced, every setting hand-finished, every detail considered. This is jewelry made to be worn, loved, and passed down.</p>
      <div class="stats">
        <div><div class="stat-num">38+</div><div class="stat-label">Years of Craft</div></div>
        <div><div class="stat-num">12K</div><div class="stat-label">Happy Clients</div></div>
        <div><div class="stat-num">100%</div><div class="stat-label">Ethical Sourcing</div></div>
      </div>
    </div>
  </div>
</section>

<!-- Testimonials -->
<section class="testimonials" id="reviews">
  <div class="container">
    <div class="center reveal">
      <span class="eyebrow">Client Stories</span>
      <h2 class="section-title">Loved by Thousands</h2>
      <p class="section-sub">Real words from the people who wear our jewelry every day.</p>
    </div>
    <div class="testi-grid reveal">
      <div class="testi">
        <div class="stars">★★★★★</div>
        <p>"The engagement ring exceeded every expectation. The craftsmanship is extraordinary, and the service was deeply personal."</p>
        <div class="testi-author">
          <div class="avatar">S</div>
          <div><strong>Sophia Laurent</strong><span>Paris, France</span></div>
        </div>
      </div>
      <div class="testi">
        <div class="stars">★★★★★</div>
        <p>"I've purchased several pieces over the years. Each one feels like a small work of art. Truly a brand you can trust."</p>
        <div class="testi-author">
          <div class="avatar">E</div>
          <div><strong>Emma Chen</strong><span>New York, USA</span></div>
        </div>
      </div>
      <div class="testi">
        <div class="stars">★★★★★</div>
        <p>"The pearl earrings I received as a gift are breathtaking. The packaging alone felt like opening a treasure chest."</p>
        <div class="testi-author">
          <div class="avatar">A</div>
          <div><strong>Amara Okafor</strong><span>London, UK</span></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Newsletter -->
<section class="newsletter">
  <div class="container reveal">
    <span class="eyebrow">Join the Circle</span>
    <h2 class="section-title" style="color:white">Receive 10% Off Your First Order</h2>
    <p>Be the first to know about new collections, private events, and exclusive offers.</p>
    <form class="newsletter-form" onsubmit="subscribeNewsletter(event)">
      <input type="email" placeholder="Your email address" required>
      <button type="submit">Subscribe</button>
    </form>
  </div>
</section>

<!-- Footer -->
<footer id="contact">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href="#" class="logo">LUMI<span>È</span>RE</a>
        <p>Timeless fine jewelry, ethically crafted in our ateliers across Europe. Every piece, a legacy.</p>
        <div class="socials">
          <a href="#" aria-label="Instagram"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor"/></svg></a>
          <a href="#" aria-label="Facebook"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M13 22v-8h3l1-4h-4V7c0-1 .5-2 2-2h2V1h-3c-3 0-5 2-5 5v4H6v4h3v8h4Z"/></svg></a>
          <a href="#" aria-label="Pinterest"><svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 